server:
  port: 8085 # Port for the account-service

spring:
  application:
    name: creditcard-service # Name of the service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # URL of the Eureka Server
